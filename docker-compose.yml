version: '3.8'

services:
  bot:
    image: node:lts-alpine
    working_dir: /app
    volumes:
      - ./:/app
      - /app/node_modules
    command: npm run dev
    env_file: .env
    restart: unless-stopped
